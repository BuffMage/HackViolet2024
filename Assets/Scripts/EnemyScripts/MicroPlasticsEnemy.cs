using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MicroPlasticsEnemy : Enemy
{
    // Start is called before the first frame update
    void Start()
    {
        health = 5;
        speed = 2.0f;
        attackSpeed = 1f;
        strength = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
